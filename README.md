# TODO

This is an example TODO app!

## Running

1. Clone the repository - `git clone https://github.com/jackprice/terrible-todo.git && cd terrible-todo`
2. Install dependencies - `npm install`
3. Start the app - `yarn start`